# 86 — Cómo continuar (2026-08-04)

> **✎ 2026-08-04 (tarde/noche): SUPERADO en el mismo día.** La campaña D1 se reanudó y
> completó, y detrás corrieron H1, B1 y G1 (docs 85 §4–7, 87, 88, 89): **el eje de la
> tesis quedó cerrado y las palancas del banco agotadas**. La tabla "Después de D1" de
> abajo quedó vieja. **El punto de entrada vigente es el doc 90** (tablero de
> decisiones del usuario) + `results/clip_bench/index.md` (la tabla del informe con
> las 6 campañas). Este doc se conserva como registro del corte de esa mañana.

Nota de contexto para retomar. **Reemplaza al doc 82** como punto de entrada
operativo: todo lo que ese doc listaba como "lo siguiente" en los items 1–2 está
ejecutado; el corte fue a mitad de la campaña D1 (reanudable).

## Dónde estamos

En la jornada 2026-08-03/04 (docs 83, 84, 85) se ejecutó el **eje central de la
tesis** hasta dejarlo a una campaña de distancia:

- **Fase D, Fase 1 (Nivel A) — CORRIDA** (doc 83). El gate pre-registrado NO se
  dispara (CR-01 ratio 0,34–0,46 en dos estratos; CR-02 0,87) → **E-DIR pasa a
  Fase 2**. GT de CR-02 a nivel persona creado desde negativos explícitos del raw
  (D10 respetado; CHV descartado por circularidad). E-HYB Fase 1 offline: -or no
  supera a E-IND en ningún corte; corroboración discrimina 2,4–3,0× en CR-01,
  CR-02 indeterminado (F-83.7 corregido por la réplica).
- **Réplica base-560 — COMPLETA** (doc 84). **F-84.1: la debilidad de E-DIR es
  estructural** (ratio 0,34 con ambos modelos, IC no solapados). T2 clips:
  F-81.2b refutada bajo `v2_short` (el pre-roll empeora), **pero CR-02 SDR
  0,281→0,920 y t_alert −2,2 s** (F-84.6, el par de F-81.1). La vía
  `bare_head`-como-evidencia (base 0,599 vs 0,308) quedó sin probar — requiere
  `direct_evidence`.
- **Evaluador `direct_evidence` — IMPLEMENTADO** (doc 85, spec 41 §6.1 tal cual).
  Control-plane **285 passed** (+21, TDD). `evidence.strategy: eind|edir|hyb_or`
  (`hyb_and` rechazada en validación hasta el tramo de fusiones). Dos fixes del
  humo: **F-85.1** umbrales de imágenes no transfieren a video (→ 0,25 default de
  plataforma) y **F-85.2** el derivador SDR/TTFD hardcodeaba spatial_absence
  (→ despacha por estrategia; 4º artefacto familia F-EV).

## LO PRIMERO al retomar: reanudar la campaña D1 (quedó en 2/34)

Se detuvo a pedido del usuario con 2/34 clips. El runner es reanudable
(saltea lo hecho por `resultados.json`):

```bash
# 1. Levantar el media-plane DESDE SU REPO con el campeón
cd e-ovrt_media-plane && EOVRT_MODEL_REF=grounding-dino/gdino-tiny-560 \
  .venv/bin/uvicorn --factory eovrt_media.service.app:create_app --port 8080
# 2. Reanudar (32 clips restantes, ~80 min a ~150 s/clip — el caption E-DIR
#    es ~1,7x más lento que el de T1 por los tokens de texto)
python3 docs/operacion/datos/85-ciclo-edir-runner.py
# 3. Al terminar: agregar y volcar la fila
cd e-ovrt_datasets && python3 datasets/scripts/bench/aggregate_clip_campaign.py \
  --evals-dir ../docs/operacion/datos/85-d1-edir-clips \
  --gt-dir datasets-videos/gt \
  --campaign ../e-ovrt_experimental-setup/results/clip_bench/d1_gdinotiny560_edirpair_scene/campaign.yaml \
  --out      ../e-ovrt_experimental-setup/results/clip_bench/d1_gdinotiny560_edirpair_scene/metrics.json
# 4. Fila en results/clip_bench/index.md + cerrar doc 85 §4 + evals a results/
```

Pista de lectura ya medida (humo, `a_p1_c09`): E-DIR confirma a los 4,0 s con
evidencia desde el frame 0 — la ceguera al atributo del Nivel A apareciendo como
confirmación prematura. La campaña dice si es el patrón general.

## Después de D1, en orden

| # | Qué | Estado |
|---|---|---|
| 1 | **Fusiones Nivel B**: `hyb_or` ya corre (es config); `hyb_and` = marca de corroboración + factor de ventana en el motor (spec 41 §6.2). F-83.7 corregido: corroboración validada solo en CR-01 | `hyb_and` pendiente |
| 2 | **`bare_head` × base-560 vía `direct_evidence`** (`match: region_center`) — la vía que T2 no probó (F-84.5), mayor separación entre modelos | habilitada |
| 3 | G0 vs G1 en P7 (doc 79) — la otra palanca para P7–P9 | requiere `track_id` |
| 4 | Lote de internet (14 clips) → soak/FAR (L1) | espera CVAT (usuario) |
| 5 | **COMMIT**: docs 83–86, datasets (+56 tests, 3 módulos bench), control-plane (evaluador +21 tests, pattern set edir), experimental-setup (results/bench_nivel_a + clip_bench T2/D1) | usuario pide la secuencia |
| 6 | Backup de `docs` (git bundle) + administrativo del rodaje | usuario |

## Decisiones tomadas hoy que NO se re-litigan

- **CHV no da GT de CR-02** (circularidad D10: derivar ausencia por geometría ES
  la operación de E-IND). El GT válido sale de `NO-Safety Vest` del raw.
- **Umbral de evidencia directa = 0,25** (default de plataforma, trato simétrico
  con E-IND). No se calibra sobre los clips: son el test set.
- **El brazo E-IND de la Fase 2 es T1** (caption `eind_v1` byte-idéntico a
  `v2_short`): se declara la equivalencia, no se re-infiere.
- **Variantes E-DIR de Nivel B**: `cr01_spec` + `cr02_obs` (ganadoras por F1 de
  calibración en mitad A de `bench_obra`, elegidas ANTES de mirar la mitad B).

## Trampas nuevas de esta jornada (suman a las 6 del doc 82)

7. Reusar corridas viejas en experimentos de prompts: chequear `prompt_set_id`
   (el caption de GDINO lleva TODAS las frases activas).
8. Grid de calibración con el óptimo en el borde no calibró nada.
9. El universo de scoring es el estrato, no las imágenes con GT de persona.
10. `run_descriptor` no guarda la variante del modelo (D-61.4) — declarar la
    procedencia en campaign.yaml.
11. Un runner reanudable con salida fija "salta" una réplica entera (la clave de
    reanudación no incluye el modelo) — usar `--out-dir` nuevo por combinación.
12. **Umbrales calibrados no viajan entre dominios ni captions** (F-85.1).
13. **Todo derivador de métricas que instancie un evaluador concreto es una bomba
    silenciosa** cuando aparece una estrategia nueva (F-85.2): usar el despacho.

## Estado de los planos

Media-plane y control-plane **apagados**, GPU liberada (0 MiB). Nada corriendo.
Suites: control-plane **285**, datasets **272** — todas verdes. **Nada commiteado**
(regla: solo a pedido explícito).
