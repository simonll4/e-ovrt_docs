# Borradores de las secciones que faltan

Lugar declarado por el manual de aplicación (`ajustes/08` §2, decisión D-A) para el patrón *texto
listo para copiar*: se redacta acá y se pega en el `.docx` de la sección, nunca al revés.

**Está vacía a propósito.** Los dos borradores que hubo, el de §17.4 y la vara del §15, ya se
pegaron y quedaron en [`../archivado/`](../archivado/00-que-hay-aca.md) el 2026-09-07. El texto
vigente de esas dos secciones vive en su `.docx` de `../desarrollando/` y en su foto extraída.

Las secciones que todavía pueden pasar por acá son **§17.6, §18 y §19**, las tres sin redactar.

> **No confundir con la foto extraída.** Los `90*` y los `96*` son extracciones de un `.docx`, no
> material de redacción: se leen y se citan, no se editan.

> ✎ **2026-09-08.** Acá aparecieron cinco `.docx` de secciones, copiados junto con una bajada de Drive.
> Se borraron: eran duplicados exactos de los que ya estaban en `../desarrollando/`, verificados por
> sha256. **Un `.docx` exportado nunca va acá**; su lugar es `../desarrollando/` si es la versión
> vigente, o `../desarrollando/archivado/` si quedó superada.
